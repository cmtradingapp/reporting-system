version: "3.8"

services:
  app:
    build: .
    network_mode: host
    env_file: .env
    restart: unless-stopped
    volumes:
      - /root/.postgresql:/root/.postgresql:ro
      - ./app:/app/app:ro
